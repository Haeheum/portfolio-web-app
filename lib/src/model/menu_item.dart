import 'package:flutter/widgets.dart';

class MenuItem {
  final String title;
  final IconData icon;
  final double offset;

  const MenuItem({
    required this.title,
    required this.icon,
    required this.offset,
  });
}
