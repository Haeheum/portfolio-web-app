{
  "sksl": {
    "entrypoint": "nightVision_fragment_main",
    "shader": "// This SkSL shader is autogenerated by spirv-cross.\n\nfloat4 flutter_FragCoord;\n\nuniform vec2 iResolution;\nuniform float iTime;\nuniform shader iChannel0;\nuniform half2 iChannel0_size;\n\nvec4 fragColor;\n\nfloat FLT_flutter_local_RGB2Luminance(vec3 rgb)\n{\n    return ((0.2125999927520751953125 * rgb.x) + (0.715200006961822509765625 * rgb.y)) + (0.072200000286102294921875 * rgb.z);\n}\n\nvoid FLT_main()\n{\n    vec2 uv = flutter_FragCoord.xy / iResolution;\n    vec2 pixelSize = vec2(1.0) / iResolution;\n    vec3 param = iChannel0.eval(iChannel0_size * ( uv + (-pixelSize))).xyz;\n    float tl = FLT_flutter_local_RGB2Luminance(param);\n    vec3 param_1 = iChannel0.eval(iChannel0_size * ( uv + vec2(0.0, -pixelSize.y))).xyz;\n    float t = FLT_flutter_local_RGB2Luminance(param_1);\n    vec3 param_2 = iChannel0.eval(iChannel0_size * ( uv + vec2(pixelSize.x, -pixelSize.y))).xyz;\n    float tr = FLT_flutter_local_RGB2Luminance(param_2);\n    vec3 param_3 = iChannel0.eval(iChannel0_size * ( uv + vec2(-pixelSize.x, 0.0))).xyz;\n    float cl = FLT_flutter_local_RGB2Luminance(param_3);\n    vec3 param_4 = iChannel0.eval(iChannel0_size * ( uv)).xyz;\n    float c = FLT_flutter_local_RGB2Luminance(param_4);\n    vec3 param_5 = iChannel0.eval(iChannel0_size * ( uv + vec2(pixelSize.x, 0.0))).xyz;\n    float cr = FLT_flutter_local_RGB2Luminance(param_5);\n    vec3 param_6 = iChannel0.eval(iChannel0_size * ( uv + vec2(-pixelSize.x, pixelSize.y))).xyz;\n    float bl = FLT_flutter_local_RGB2Luminance(param_6);\n    vec3 param_7 = iChannel0.eval(iChannel0_size * ( uv + vec2(0.0, pixelSize.y))).xyz;\n    float b = FLT_flutter_local_RGB2Luminance(param_7);\n    vec3 param_8 = iChannel0.eval(iChannel0_size * ( uv + vec2(pixelSize.x, pixelSize.y))).xyz;\n    float br = FLT_flutter_local_RGB2Luminance(param_8);\n    float sobelX = (((((tl * (-1.0)) + (tr * 1.0)) + (cl * (-2.0))) + (cr * 2.0)) + (bl * (-1.0))) + (br * 1.0);\n    float sobelY = (((((tl * (-1.0)) + (t * (-2.0))) + (tr * (-1.0))) + (bl * 1.0)) + (b * 2.0)) + (br * 1.0);\n    float sobel = sqrt((sobelX * sobelX) + (sobelY * sobelY));\n    vec4 textureColor = iChannel0.eval(iChannel0_size * ( uv));\n    vec3 param_9 = textureColor.xyz;\n    float luminance = FLT_flutter_local_RGB2Luminance(param_9);\n    vec3 nightVisionColor = vec3(0.0, 1.0, 0.0) * luminance;\n    float sobelFactor = smoothstep(0.0, 0.699999988079071044921875, sobel);\n    vec3 edgeColor = mix(vec3(0.0), vec3(0.0, 1.0, 0.0), vec3(sobelFactor));\n    vec3 finalColor = mix(nightVisionColor, edgeColor, vec3(sobelFactor));\n    fragColor = vec4(finalColor, 1.0);\n}\n\nhalf4 main(float2 iFragCoord)\n{\n      flutter_FragCoord = float4(iFragCoord, 0, 0);\n      FLT_main();\n      return fragColor;\n}\n",
    "stage": 1,
    "uniforms": [
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 0,
        "name": "iResolution",
        "rows": 2,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 32,
        "columns": 1,
        "location": 1,
        "name": "iTime",
        "rows": 1,
        "type": 10
      },
      {
        "array_elements": 0,
        "bit_width": 0,
        "columns": 1,
        "location": 2,
        "name": "iChannel0",
        "rows": 1,
        "type": 12
      }
    ]
  }
}